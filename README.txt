This was a rather simple project I put together because I was curious about oversubscription ratios on my VMware lab at home.
Obviously, you will need to change the IP for vCenter to match the IP of your vCenter host.  You will also need PowerCLI installed.

Other than that, it should simply run and output information on any cluster it finds in vCenter that the supplied credentials have permissions to.
